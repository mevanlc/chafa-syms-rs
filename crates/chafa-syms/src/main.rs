//! `chafa-syms` — demo/test CLI over [`chafa_syms_rs`].
//!
//! Implemented in Phase 8. For now this is a placeholder so the workspace builds.

fn main() {
    eprintln!("chafa-syms: CLI not yet implemented (see devdocs/PLAN.md, Phase 8)");
    std::process::exit(2);
}
