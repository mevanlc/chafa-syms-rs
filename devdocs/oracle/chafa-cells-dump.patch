diff --git a/chafa/chafa-canvas.c b/chafa/chafa-canvas.c
index 587764c..cc43fec 100644
--- a/chafa/chafa-canvas.c
+++ b/chafa/chafa-canvas.c
@@ -20,6 +20,7 @@
 #include "config.h"
 
 #include <math.h>
+#include <stdio.h>
 #include <string.h>
 #include <glib.h>
 #include "chafa.h"
@@ -846,6 +847,42 @@ chafa_canvas_build_ansi (ChafaCanvas *canvas)
     return chafa_canvas_print (canvas, NULL);
 }
 
+/* chafa-syms-rs parity oracle: dump per-cell (codepoint, fg_color, bg_color)
+ * to the file named by the CHAFA_DUMP_CELLS env var. Exposes the symbol
+ * renderer's internal cell state directly, bypassing all printer encoding
+ * (SGR 7 inversion, space optimization, etc.), so the Rust port can validate
+ * its selection core against chafa's actual picks. No effect unless the env
+ * var is set and the canvas is in symbol mode. */
+static void
+chafa_syms_rs_dump_cells (ChafaCanvas *canvas)
+{
+    const gchar *dump_path = g_getenv ("CHAFA_DUMP_CELLS");
+    FILE *df;
+    gint cx, cy, w, h;
+
+    if (!dump_path || canvas->config.pixel_mode != CHAFA_PIXEL_MODE_SYMBOLS || !canvas->cells)
+        return;
+
+    df = fopen (dump_path, "w");
+    if (!df)
+        return;
+
+    w = canvas->config.width;
+    h = canvas->config.height;
+    fprintf (df, "%d %d\n", w, h);
+    for (cy = 0; cy < h; cy++)
+    {
+        for (cx = 0; cx < w; cx++)
+        {
+            ChafaCanvasCell *cell = &canvas->cells [(gsize) cy * (gsize) w + cx];
+            fprintf (df, "%d %d %u %08x %08x\n",
+                     cx, cy, (unsigned) cell->c,
+                     (unsigned) cell->fg_color, (unsigned) cell->bg_color);
+        }
+    }
+    fclose (df);
+}
+
 /**
  * chafa_canvas_print:
  * @canvas: The canvas to generate a printable representation of
@@ -871,6 +908,8 @@ chafa_canvas_print (ChafaCanvas *canvas, ChafaTermInfo *term_info)
     g_return_val_if_fail (canvas != NULL, NULL);
     g_return_val_if_fail (canvas->refs > 0, NULL);
 
+    chafa_syms_rs_dump_cells (canvas);
+
     if (term_info)
         chafa_term_info_ref (term_info);
     else
@@ -956,6 +995,8 @@ chafa_canvas_print_rows (ChafaCanvas *canvas, ChafaTermInfo *term_info,
     g_return_if_fail (canvas->refs > 0);
     g_return_if_fail (array_out != NULL);
 
+    chafa_syms_rs_dump_cells (canvas);
+
     if (term_info)
         chafa_term_info_ref (term_info);
     else
diff --git a/chafa/internal/chafa-symbols.c b/chafa/internal/chafa-symbols.c
index bebfe2e..df3d014 100644
--- a/chafa/internal/chafa-symbols.c
+++ b/chafa/internal/chafa-symbols.c
@@ -19,6 +19,7 @@
 
 #include "config.h"
 
+#include <stdio.h>
 #include <string.h>  /* memset */
 #include "chafa.h"
 #include "internal/chafa-private.h"
@@ -637,6 +638,52 @@ init_symbol_array_wide (const ChafaSymbolDef *defs)
     return syms;
 }
 
+/* chafa-syms-rs parity oracle: dump the fully-initialized builtin symbol
+ * arrays (narrow + wide) to the file named by CHAFA_DUMP_SYMBOLS. This is the
+ * ground truth the Rust port's symbol layer (parsed outlines + ported
+ * braille/sextant/octant generators + tag derivation) is validated against,
+ * neutralizing the GLib-Unicode-version parity risk. No effect unless set. */
+static void
+chafa_syms_rs_dump_symbols (void)
+{
+    const gchar *path = g_getenv ("CHAFA_DUMP_SYMBOLS");
+    FILE *df;
+    gint n, m, i;
+
+    if (!path)
+        return;
+
+    df = fopen (path, "w");
+    if (!df)
+        return;
+
+    for (n = 0; chafa_symbols [n].c; n++)
+        ;
+    for (m = 0; chafa_symbols2 [m].sym [0].c; m++)
+        ;
+
+    fprintf (df, "NARROW %d\n", n);
+    for (i = 0; i < n; i++)
+    {
+        ChafaSymbol *s = &chafa_symbols [i];
+        fprintf (df, "%u %u %d %016" G_GINT64_MODIFIER "x\n",
+                 (unsigned) s->c, (unsigned) s->sc, s->popcount,
+                 (guint64) s->bitmap);
+    }
+
+    fprintf (df, "WIDE %d\n", m);
+    for (i = 0; i < m; i++)
+    {
+        ChafaSymbol2 *s = &chafa_symbols2 [i];
+        fprintf (df, "%u %u %d %d %016" G_GINT64_MODIFIER "x %016" G_GINT64_MODIFIER "x\n",
+                 (unsigned) s->sym [0].c, (unsigned) s->sym [0].sc,
+                 s->sym [0].popcount, s->sym [1].popcount,
+                 (guint64) s->sym [0].bitmap, (guint64) s->sym [1].bitmap);
+    }
+
+    fclose (df);
+}
+
 void
 chafa_init_symbols (void)
 {
@@ -647,6 +694,8 @@ chafa_init_symbols (void)
     chafa_symbols2 = init_symbol_array_wide (symbol_defs);
 
     symbols_initialized = TRUE;
+
+    chafa_syms_rs_dump_symbols ();
 }
 
 ChafaSymbolTags
