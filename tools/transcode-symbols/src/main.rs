//! `transcode-symbols` — one-shot generator (run manually; output committed).
//!
//! Parses the `CHAFA_SYMBOL_OUTLINE_8X8 / 16X8(...)` outline tables from
//! `chafa-symbols-{ascii,latin,block,kana,misc-narrow}.h` into Rust arrays
//! written to `crates/chafa-syms-rs/src/symbol/data.rs`.
//!
//! Implemented in Phase 2.

fn main() {
    eprintln!("transcode-symbols: not yet implemented (see devdocs/PLAN.md, Phase 2)");
    std::process::exit(2);
}
